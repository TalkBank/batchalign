# Evaluate analysis results

The current public CLI does not register `eval l2-morphotag`. Earlier examples
of that command refer to a retired evaluation harness.

For transcription comparison, use [compare](compare.md). For morphology,
inspect the generated `%mor` and `%gra` tiers against checked examples from
your language and workflow. Historical evaluation data is not a guarantee of
accuracy for the current backend or corpus.
