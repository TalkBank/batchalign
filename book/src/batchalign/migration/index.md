# Migrate an existing Batchalign workflow

Start with [Update commands and integrations](user-migration.md). It maps the
current CLI and Python API without assuming the retired Rust server architecture.

For current behavior, use the [CLI reference](../user-guide/cli-reference.md),
[Python API](../user-guide/python-api.md), and
[pipeline explanation](../architecture/command-lifecycles.md).

Earlier migration studies and model experiments are
[historical reference material](../reference/historical-notes.md). They describe
particular earlier implementations and are not current command instructions.
